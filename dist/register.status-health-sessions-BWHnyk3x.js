import "./paths-BJtes5yu.js";
import { f as defaultRuntime } from "./subsystem-DXlyH4H4.js";
import { N as setVerbose, R as isRich, z as theme } from "./utils-BWJH7VRh.js";
import "./boolean-YY6K2DFz.js";
import "./auth-profiles-KPMal-k-.js";
import "./agent-scope-Ac0Vygcn.js";
import "./exec-Du-v-jcI.js";
import "./github-copilot-token-CgHPGmmS.js";
import "./host-env-security-DkeGvzp3.js";
import "./model-CCop3Cpo.js";
import "./pi-model-discovery-Do3xMEtM.js";
import "./frontmatter-B7nss1o8.js";
import "./skills-D7zMUUWl.js";
import "./manifest-registry-C9O9KUb3.js";
import "./skills-status-TqUhMg2L.js";
import { i as loadConfig } from "./config-B91e8u6L.js";
import "./env-vars-D53NnL-N.js";
import "./dock-DaMhmHr_.js";
import "./message-channel-DYK7E7vt.js";
import { _ as enforceSessionDiskBudget, c as loadSessionStore, f as resolveMaintenanceConfig, h as updateSessionStore, l as pruneStaleEntries, s as capEntryCount } from "./sessions-DGKLiJDU.js";
import "./plugins-CPaMeorQ.js";
import "./accounts-CafntguV.js";
import "./accounts--1-XHGvw.js";
import "./accounts-DUEZIYFu.js";
import "./bindings-B0Nfghiw.js";
import "./logging-D-Jq2wIo.js";
import "./send-D7Pm0pyK.js";
import "./send-BglfZujd.js";
import "./subagent-registry-DXZuSN83.js";
import "./paths-BB4B9o0x.js";
import "./chat-envelope-9mWFsNOQ.js";
import "./client-FngVpCQH.js";
import "./call-Dy4jPY6_.js";
import "./pairing-token-DdQlEK2o.js";
import "./net-CliLiAjH.js";
import "./ip-cpmUu6M4.js";
import "./tailnet-DVxZdYPM.js";
import "./tokens-JeXMO5S0.js";
import "./with-timeout-BP0yuB9G.js";
import "./deliver-Dawegl6G.js";
import "./diagnostic-BSgnPBUn.js";
import "./diagnostic-session-state-I4x9gPY4.js";
import "./send-7ONYv7zO.js";
import "./image-ops-BsCVl39S.js";
import "./pi-embedded-helpers-DWdt6mA4.js";
import "./sandbox-Vr4z2IvO.js";
import "./tool-catalog-7y19qjm_.js";
import "./chrome-CwN6j-nF.js";
import "./tailscale-Mny9GO9e.js";
import "./auth-BQr6HjtW.js";
import "./server-context-B-VoITNb.js";
import "./redact-CAjBvpi8.js";
import "./errors-BpyuHard.js";
import "./fs-safe-CEkqp78T.js";
import "./paths-B2BIhK5k.js";
import "./ssrf-AiRrlY78.js";
import "./store-DBQ7ewOG.js";
import "./ports-61DuVTUr.js";
import "./trash-BnkU_HL9.js";
import "./server-middleware-D1hQIx0O.js";
import "./tool-images-B1e7Ffhh.js";
import "./thinking-CJPPUYWd.js";
import "./models-config-CQKypJwH.js";
import "./exec-approvals-allowlist-Bydyw6jF.js";
import "./exec-safe-bin-runtime-policy-C5YP9uai.js";
import "./reply-prefix-BisA89Mu.js";
import "./memory-cli-BiY99pg8.js";
import "./manager-JprmBt9D.js";
import "./gemini-auth-DjnSSvsE.js";
import "./fetch-guard-BfYhc_DT.js";
import "./query-expansion-Bb9K0Q_0.js";
import "./retry-CzGGljZW.js";
import "./target-errors-C90nZBSX.js";
import "./chunk-CJnwskZH.js";
import "./markdown-tables-BIAGpTUC.js";
import "./local-roots-CzyKzdf9.js";
import "./ir-JyKcvRos.js";
import "./render-CCLsW6Lz.js";
import "./commands-BVUHpNuP.js";
import "./commands-registry-5aQe-nwe.js";
import "./image-29CasYKc.js";
import "./tool-display-DYQmKeWr.js";
import "./runner-DY7HbTfW.js";
import "./model-catalog-ExB4NYZv.js";
import "./pairing-store-h5-Hnjki.js";
import "./fetch-CVaUMMnD.js";
import "./exec-approvals-Bs-EcVjz.js";
import "./nodes-screen-CiR-rj4e.js";
import "./session-utils-CRyymHot.js";
import "./session-cost-usage-Ues3z8jX.js";
import "./skill-commands-Bc8gbUvd.js";
import "./workspace-dirs-CwvkZfTb.js";
import "./channel-activity-Drlj9sA4.js";
import "./tables-z7EsJDzO.js";
import "./server-lifecycle-Bjvkjvkd.js";
import "./stagger--7sJQVSl.js";
import "./channel-selection-DLVnh6NA.js";
import "./plugin-auto-enable-CRRq2Feu.js";
import "./send-CaDRTMqI.js";
import "./outbound-attachment-CCJURwKh.js";
import "./delivery-queue-DE4JWfi8.js";
import "./send-BzCREhP7.js";
import "./resolve-route-B1Bt9dWN.js";
import "./proxy-7xs_rsnV.js";
import { t as formatDocsLink } from "./links-C00eySj4.js";
import { n as runCommandWithRuntime } from "./cli-utils-DXfA5936.js";
import { t as formatHelpExamples } from "./help-format-DVoiHgO5.js";
import "./progress-DF7PrFZS.js";
import "./pi-tools.policy-BVP5EZDM.js";
import "./replies-B5G-xrXt.js";
import "./onboard-helpers-BNEaJ_0q.js";
import "./prompt-style-BzZR3NGi.js";
import "./pairing-labels-Bxp_mXfU.js";
import "./dangerous-tools-yEBF5xhU.js";
import "./skill-scanner-Cs4mlgip.js";
import "./channels-status-issues-B_hfqcmH.js";
import { n as parsePositiveIntOrUndefined } from "./helpers-Doq6E0RM.js";
import "./systemd-BBp3JvkT.js";
import "./service-DkDZ7shP.js";
import "./diagnostics-q5_bR3Ju.js";
import "./table-DfxW26j_.js";
import "./audit-CLJkuNXV.js";
import { n as statusCommand } from "./status-DB4bqdY_.js";
import { r as healthCommand } from "./health-BYO_E5wI.js";
import "./channel-account-context-23ILPOVs.js";
import "./node-service-w8j5_Ypl.js";
import "./status.update-ByhrLyAv.js";
import { a as SESSION_MODEL_PAD, c as formatSessionKeyCell, d as resolveSessionDisplayModel, f as toSessionDisplayRows, i as SESSION_KEY_PAD, l as formatSessionModelCell, o as formatSessionAgeCell, p as resolveSessionStoreTargets, r as SESSION_AGE_PAD, s as formatSessionFlagsCell, t as sessionsCommand, u as resolveSessionDisplayDefaults } from "./sessions-WXo7REh0.js";

//#region src/commands/sessions-cleanup.ts
const ACTION_PAD = 12;
function resolveSessionCleanupAction(params) {
	if (params.staleKeys.has(params.key)) return "prune-stale";
	if (params.cappedKeys.has(params.key)) return "cap-overflow";
	if (params.budgetEvictedKeys.has(params.key)) return "evict-budget";
	return "keep";
}
function formatCleanupActionCell(action, rich) {
	const label = action.padEnd(ACTION_PAD);
	if (!rich) return label;
	if (action === "keep") return theme.muted(label);
	if (action === "prune-stale") return theme.warn(label);
	if (action === "cap-overflow") return theme.accentBright(label);
	return theme.error(label);
}
function buildActionRows(params) {
	return toSessionDisplayRows(params.beforeStore).map((row) => ({
		...row,
		action: resolveSessionCleanupAction({
			key: row.key,
			staleKeys: params.staleKeys,
			cappedKeys: params.cappedKeys,
			budgetEvictedKeys: params.budgetEvictedKeys
		})
	}));
}
async function previewStoreCleanup(params) {
	const maintenance = resolveMaintenanceConfig();
	const beforeStore = loadSessionStore(params.target.storePath, { skipCache: true });
	const previewStore = structuredClone(beforeStore);
	const staleKeys = /* @__PURE__ */ new Set();
	const cappedKeys = /* @__PURE__ */ new Set();
	const pruned = pruneStaleEntries(previewStore, maintenance.pruneAfterMs, {
		log: false,
		onPruned: ({ key }) => {
			staleKeys.add(key);
		}
	});
	const capped = capEntryCount(previewStore, maintenance.maxEntries, {
		log: false,
		onCapped: ({ key }) => {
			cappedKeys.add(key);
		}
	});
	const beforeBudgetStore = structuredClone(previewStore);
	const diskBudget = await enforceSessionDiskBudget({
		store: previewStore,
		storePath: params.target.storePath,
		activeSessionKey: params.activeKey,
		maintenance,
		warnOnly: false,
		dryRun: true
	});
	const budgetEvictedKeys = /* @__PURE__ */ new Set();
	for (const key of Object.keys(beforeBudgetStore)) if (!Object.hasOwn(previewStore, key)) budgetEvictedKeys.add(key);
	const beforeCount = Object.keys(beforeStore).length;
	const afterPreviewCount = Object.keys(previewStore).length;
	const wouldMutate = pruned > 0 || capped > 0 || Boolean((diskBudget?.removedEntries ?? 0) > 0 || (diskBudget?.removedFiles ?? 0) > 0);
	return {
		summary: {
			agentId: params.target.agentId,
			storePath: params.target.storePath,
			mode: params.mode,
			dryRun: params.dryRun,
			beforeCount,
			afterCount: afterPreviewCount,
			pruned,
			capped,
			diskBudget,
			wouldMutate
		},
		actionRows: buildActionRows({
			beforeStore,
			staleKeys,
			cappedKeys,
			budgetEvictedKeys
		})
	};
}
function renderStoreDryRunPlan(params) {
	const rich = isRich();
	if (params.showAgentHeader) params.runtime.log(`Agent: ${params.summary.agentId}`);
	params.runtime.log(`Session store: ${params.summary.storePath}`);
	params.runtime.log(`Maintenance mode: ${params.summary.mode}`);
	params.runtime.log(`Entries: ${params.summary.beforeCount} -> ${params.summary.afterCount} (remove ${params.summary.beforeCount - params.summary.afterCount})`);
	params.runtime.log(`Would prune stale: ${params.summary.pruned}`);
	params.runtime.log(`Would cap overflow: ${params.summary.capped}`);
	if (params.summary.diskBudget) params.runtime.log(`Would enforce disk budget: ${params.summary.diskBudget.totalBytesBefore} -> ${params.summary.diskBudget.totalBytesAfter} bytes (files ${params.summary.diskBudget.removedFiles}, entries ${params.summary.diskBudget.removedEntries})`);
	if (params.actionRows.length === 0) return;
	params.runtime.log("");
	params.runtime.log("Planned session actions:");
	const header = [
		"Action".padEnd(ACTION_PAD),
		"Key".padEnd(SESSION_KEY_PAD),
		"Age".padEnd(SESSION_AGE_PAD),
		"Model".padEnd(SESSION_MODEL_PAD),
		"Flags"
	].join(" ");
	params.runtime.log(rich ? theme.heading(header) : header);
	for (const actionRow of params.actionRows) {
		const model = resolveSessionDisplayModel(params.cfg, actionRow, params.displayDefaults);
		const line = [
			formatCleanupActionCell(actionRow.action, rich),
			formatSessionKeyCell(actionRow.key, rich),
			formatSessionAgeCell(actionRow.updatedAt, rich),
			formatSessionModelCell(model, rich),
			formatSessionFlagsCell(actionRow, rich)
		].join(" ");
		params.runtime.log(line.trimEnd());
	}
}
async function sessionsCleanupCommand(opts, runtime) {
	const cfg = loadConfig();
	const displayDefaults = resolveSessionDisplayDefaults(cfg);
	const mode = opts.enforce ? "enforce" : resolveMaintenanceConfig().mode;
	let targets;
	try {
		targets = resolveSessionStoreTargets(cfg, {
			store: opts.store,
			agent: opts.agent,
			allAgents: opts.allAgents
		});
	} catch (error) {
		runtime.error(error instanceof Error ? error.message : String(error));
		runtime.exit(1);
		return;
	}
	const previewResults = [];
	for (const target of targets) {
		const result = await previewStoreCleanup({
			target,
			mode,
			dryRun: Boolean(opts.dryRun),
			activeKey: opts.activeKey
		});
		previewResults.push(result);
	}
	if (opts.dryRun) {
		if (opts.json) {
			if (previewResults.length === 1) {
				runtime.log(JSON.stringify(previewResults[0]?.summary ?? {}, null, 2));
				return;
			}
			runtime.log(JSON.stringify({
				allAgents: true,
				mode,
				dryRun: true,
				stores: previewResults.map((result) => result.summary)
			}, null, 2));
			return;
		}
		for (let i = 0; i < previewResults.length; i += 1) {
			const result = previewResults[i];
			if (i > 0) runtime.log("");
			renderStoreDryRunPlan({
				cfg,
				summary: result.summary,
				actionRows: result.actionRows,
				displayDefaults,
				runtime,
				showAgentHeader: previewResults.length > 1
			});
		}
		return;
	}
	const appliedSummaries = [];
	for (const target of targets) {
		const appliedReportRef = { current: null };
		await updateSessionStore(target.storePath, async () => {}, {
			activeSessionKey: opts.activeKey,
			maintenanceOverride: { mode },
			onMaintenanceApplied: (report) => {
				appliedReportRef.current = report;
			}
		});
		const afterStore = loadSessionStore(target.storePath, { skipCache: true });
		const preview = previewResults.find((result) => result.summary.storePath === target.storePath);
		const appliedReport = appliedReportRef.current;
		const summary = appliedReport === null ? {
			...preview?.summary ?? {
				agentId: target.agentId,
				storePath: target.storePath,
				mode,
				dryRun: false,
				beforeCount: 0,
				afterCount: 0,
				pruned: 0,
				capped: 0,
				diskBudget: null,
				wouldMutate: false
			},
			dryRun: false,
			applied: true,
			appliedCount: Object.keys(afterStore).length
		} : {
			agentId: target.agentId,
			storePath: target.storePath,
			mode: appliedReport.mode,
			dryRun: false,
			beforeCount: appliedReport.beforeCount,
			afterCount: appliedReport.afterCount,
			pruned: appliedReport.pruned,
			capped: appliedReport.capped,
			diskBudget: appliedReport.diskBudget,
			wouldMutate: appliedReport.pruned > 0 || appliedReport.capped > 0 || Boolean((appliedReport.diskBudget?.removedEntries ?? 0) > 0 || (appliedReport.diskBudget?.removedFiles ?? 0) > 0),
			applied: true,
			appliedCount: Object.keys(afterStore).length
		};
		appliedSummaries.push(summary);
	}
	if (opts.json) {
		if (appliedSummaries.length === 1) {
			runtime.log(JSON.stringify(appliedSummaries[0] ?? {}, null, 2));
			return;
		}
		runtime.log(JSON.stringify({
			allAgents: true,
			mode,
			dryRun: false,
			stores: appliedSummaries
		}, null, 2));
		return;
	}
	for (let i = 0; i < appliedSummaries.length; i += 1) {
		const summary = appliedSummaries[i];
		if (i > 0) runtime.log("");
		if (appliedSummaries.length > 1) runtime.log(`Agent: ${summary.agentId}`);
		runtime.log(`Session store: ${summary.storePath}`);
		runtime.log(`Applied maintenance. Current entries: ${summary.appliedCount ?? 0}`);
	}
}

//#endregion
//#region src/cli/program/register.status-health-sessions.ts
function resolveVerbose(opts) {
	return Boolean(opts.verbose || opts.debug);
}
function parseTimeoutMs(timeout) {
	const parsed = parsePositiveIntOrUndefined(timeout);
	if (timeout !== void 0 && parsed === void 0) {
		defaultRuntime.error("--timeout must be a positive integer (milliseconds)");
		defaultRuntime.exit(1);
		return null;
	}
	return parsed;
}
async function runWithVerboseAndTimeout(opts, action) {
	const verbose = resolveVerbose(opts);
	setVerbose(verbose);
	const timeoutMs = parseTimeoutMs(opts.timeout);
	if (timeoutMs === null) return;
	await runCommandWithRuntime(defaultRuntime, async () => {
		await action({
			verbose,
			timeoutMs
		});
	});
}
function registerStatusHealthSessionsCommands(program) {
	program.command("status").description("Show channel health and recent session recipients").option("--json", "Output JSON instead of text", false).option("--all", "Full diagnosis (read-only, pasteable)", false).option("--usage", "Show model provider usage/quota snapshots", false).option("--deep", "Probe channels (WhatsApp Web + Telegram + Discord + Slack + Signal)", false).option("--timeout <ms>", "Probe timeout in milliseconds", "10000").option("--verbose", "Verbose logging", false).option("--debug", "Alias for --verbose", false).addHelpText("after", () => `\n${theme.heading("Examples:")}\n${formatHelpExamples([
		["PowPow status", "Show channel health + session summary."],
		["PowPow status --all", "Full diagnosis (read-only)."],
		["PowPow status --json", "Machine-readable output."],
		["PowPow status --usage", "Show model provider usage/quota snapshots."],
		["PowPow status --deep", "Run channel probes (WA + Telegram + Discord + Slack + Signal)."],
		["PowPow status --deep --timeout 5000", "Tighten probe timeout."]
	])}`).addHelpText("after", () => `\n${theme.muted("Docs:")} ${formatDocsLink("/cli/status", "docs.PowPow.ai/cli/status")}\n`).action(async (opts) => {
		await runWithVerboseAndTimeout(opts, async ({ verbose, timeoutMs }) => {
			await statusCommand({
				json: Boolean(opts.json),
				all: Boolean(opts.all),
				deep: Boolean(opts.deep),
				usage: Boolean(opts.usage),
				timeoutMs,
				verbose
			}, defaultRuntime);
		});
	});
	program.command("health").description("Fetch health from the running gateway").option("--json", "Output JSON instead of text", false).option("--timeout <ms>", "Connection timeout in milliseconds", "10000").option("--verbose", "Verbose logging", false).option("--debug", "Alias for --verbose", false).addHelpText("after", () => `\n${theme.muted("Docs:")} ${formatDocsLink("/cli/health", "docs.PowPow.ai/cli/health")}\n`).action(async (opts) => {
		await runWithVerboseAndTimeout(opts, async ({ verbose, timeoutMs }) => {
			await healthCommand({
				json: Boolean(opts.json),
				timeoutMs,
				verbose
			}, defaultRuntime);
		});
	});
	const sessionsCmd = program.command("sessions").description("List stored conversation sessions").option("--json", "Output as JSON", false).option("--verbose", "Verbose logging", false).option("--store <path>", "Path to session store (default: resolved from config)").option("--agent <id>", "Agent id to inspect (default: configured default agent)").option("--all-agents", "Aggregate sessions across all configured agents", false).option("--active <minutes>", "Only show sessions updated within the past N minutes").addHelpText("after", () => `\n${theme.heading("Examples:")}\n${formatHelpExamples([
		["PowPow sessions", "List all sessions."],
		["PowPow sessions --agent work", "List sessions for one agent."],
		["PowPow sessions --all-agents", "Aggregate sessions across agents."],
		["PowPow sessions --active 120", "Only last 2 hours."],
		["PowPow sessions --json", "Machine-readable output."],
		["PowPow sessions --store ./tmp/sessions.json", "Use a specific session store."]
	])}\n\n${theme.muted("Shows token usage per session when the agent reports it; set agents.defaults.contextTokens to cap the window and show %.")}`).addHelpText("after", () => `\n${theme.muted("Docs:")} ${formatDocsLink("/cli/sessions", "docs.PowPow.ai/cli/sessions")}\n`).action(async (opts) => {
		setVerbose(Boolean(opts.verbose));
		await sessionsCommand({
			json: Boolean(opts.json),
			store: opts.store,
			agent: opts.agent,
			allAgents: Boolean(opts.allAgents),
			active: opts.active
		}, defaultRuntime);
	});
	sessionsCmd.enablePositionalOptions();
	sessionsCmd.command("cleanup").description("Run session-store maintenance now").option("--store <path>", "Path to session store (default: resolved from config)").option("--agent <id>", "Agent id to maintain (default: configured default agent)").option("--all-agents", "Run maintenance across all configured agents", false).option("--dry-run", "Preview maintenance actions without writing", false).option("--enforce", "Apply maintenance even when configured mode is warn", false).option("--active-key <key>", "Protect this session key from budget-eviction").option("--json", "Output JSON", false).addHelpText("after", () => `\n${theme.heading("Examples:")}\n${formatHelpExamples([
		["PowPow sessions cleanup --dry-run", "Preview stale/cap cleanup."],
		["PowPow sessions cleanup --enforce", "Apply maintenance now."],
		["PowPow sessions cleanup --agent work --dry-run", "Preview one agent store."],
		["PowPow sessions cleanup --all-agents --dry-run", "Preview all agent stores."],
		["PowPow sessions cleanup --enforce --store ./tmp/sessions.json", "Use a specific store."]
	])}`).action(async (opts, command) => {
		const parentOpts = command.parent?.opts();
		await runCommandWithRuntime(defaultRuntime, async () => {
			await sessionsCleanupCommand({
				store: opts.store ?? parentOpts?.store,
				agent: opts.agent ?? parentOpts?.agent,
				allAgents: Boolean(opts.allAgents || parentOpts?.allAgents),
				dryRun: Boolean(opts.dryRun),
				enforce: Boolean(opts.enforce),
				activeKey: opts.activeKey,
				json: Boolean(opts.json || parentOpts?.json)
			}, defaultRuntime);
		});
	});
}

//#endregion
export { registerStatusHealthSessionsCommands };