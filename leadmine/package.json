{
  "name": "leadmine",
  "version": "5.12.0",
  "private": true,
  "description": "Chrome extension that scrapes Google Maps business listings and LinkedIn people search into one downloadable file.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node --test --test-reporter=spec test/*.test.mjs",
    "icons": "node scripts/make-icons.mjs",
    "package": "node scripts/package.mjs",
    "test:dom": "node --test --test-reporter=spec test/*.e2e.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "playwright-core": "^1.63.0"
  }
}
